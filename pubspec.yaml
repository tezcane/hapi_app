name: hapi
description: An epic game that rewards and helps a Muslim perform religious acts and gain Islamic knowledge.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  # tarikh imports:
  flare_flutter: # TODO this is showing 1.5.5 in release notes but pub.dev version gives errors
    path: ./dependencies/Flare-Flutter/flare_flutter
  nima: ^1.0.5
  cupertino_icons: ^1.0.2
  flutter_markdown: ^0.6.1
  share: ^2.0.1
  intl: ^0.17.0
  shared_preferences: ^2.0.5
  rxdart: ^0.26.0
  url_launcher: ^6.0.3
  # end tarikh imports

  firebase_core: ^1.0.3
  # firebase_analytics: ^7.1.1 no nullsafe... skip it for now
  firebase_auth: ^1.1.0
  flutter_overboard: ^2.0.1
  cloud_firestore: ^1.0.5

  # google_sign_in:
  # apple_sign_in:
  get: ^4.1.4
  get_storage: ^2.0.2
  
  crypto: ^3.0.1
  http: ^0.13.1
  csv: ^5.0.0

  alquran_cloud: ^0.0.1


dependency_overrides:
  analyzer: 1.4.0
  intl: 0.17.0

dev_dependencies:
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/logo/
  fonts:
    - family: Lobster
      fonts:
        - asset: fonts/Lobster-Regular.ttf