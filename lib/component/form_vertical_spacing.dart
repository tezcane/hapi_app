import 'package:flutter/material.dart';

/// just a space in the ui. // TODO rename and use more
class FormVerticalSpace extends SizedBox {
  const FormVerticalSpace({double height = 24.0}) : super(height: height);
}
